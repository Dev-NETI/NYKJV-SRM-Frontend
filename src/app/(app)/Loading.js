const Loading = () => {
  return (
    <div className="flex min-h-screen w-full items-center justify-center bg-black opacity-0 animate-fade-in-out">
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;
